import React from 'react';

const Selection = ({ agent }) => {

    return (
        <div className="bg-dark fw-bold white p-2">
            Sales by agent: {agent}
        </div>
    )
}

export default Selection;